RewriteEngine On
# If the request is for a city node that doesn't exist as a physical folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Silently route to index.html and pass the path as a 'city' variable
RewriteRule ^nodes/([^/]+)/?$ index.html?city=$1 [L,QSA]
